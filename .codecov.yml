coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 85%

comment:
  layout: "reach, diff, flags, files"
  behavior: default
